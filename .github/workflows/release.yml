name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

env:
  FORCE_COLOR: "1"

jobs:
  validate:
    name: Validate release identity
    runs-on: ubuntu-22.04
    outputs:
      version: ${{ steps.identity.outputs.version }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"

      - name: Check tag and package metadata
        id: identity
        env:
          RELEASE_REF_NAME: ${{ github.ref_name }}
          RELEASE_REF_TYPE: ${{ github.ref_type }}
        run: |
          python - <<'PY'
          import os
          import re
          import tomllib
          from pathlib import Path

          cargo = tomllib.loads(Path("Cargo.toml").read_text())
          pyproject = tomllib.loads(Path("pyproject.toml").read_text())
          version = cargo["workspace"]["package"]["version"]
          if re.fullmatch(r"[0-9]+\.[0-9]+\.[0-9]+", version) is None:
              raise SystemExit("the release path accepts stable semantic versions only")
          project = pyproject["project"]
          if "version" in project or "version" not in project.get("dynamic", []):
              raise SystemExit("pyproject.toml must derive its version from Cargo metadata")
          if os.environ["GITHUB_EVENT_NAME"] == "workflow_dispatch":
              if os.environ["RELEASE_REF_TYPE"] != "branch" or os.environ["RELEASE_REF_NAME"] != "main":
                  raise SystemExit("TestPyPI rehearsals must run from main")
          if os.environ["RELEASE_REF_TYPE"] == "tag":
              expected_tag = f"v{version}"
              if os.environ["RELEASE_REF_NAME"] != expected_tag:
                  raise SystemExit(
                      f"release tag {os.environ['RELEASE_REF_NAME']!r} must be {expected_tag!r}"
                  )
          with Path(os.environ["GITHUB_OUTPUT"]).open("a", encoding="utf-8") as output:
              print(f"version={version}", file=output)
          PY

  distributions:
    name: Build and smoke-test distributions
    needs: validate
    uses: ./.github/workflows/native-wheels.yml
    permissions:
      contents: read

  assemble:
    name: Assemble release bundle
    needs: [validate, distributions]
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"

      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          enable-cache: false

      - name: Download candidate distributions
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: advect-distribution-*
          path: dist
          merge-multiple: true

      - name: Validate and record release provenance
        env:
          RELEASE_VERSION: ${{ needs.validate.outputs.version }}
          SOURCE_REVISION: ${{ github.sha }}
        run: |
          uv run --script scripts/assemble_release_artifacts.py \
            --dist-dir dist \
            --version "$RELEASE_VERSION" \
            --source-revision "$SOURCE_REVISION" \
            --manifest-path provenance/RELEASE-PROVENANCE.json \
            --checksums-path provenance/SHA256SUMS
          (cd dist && sha256sum --check ../provenance/SHA256SUMS)

      - name: Validate package metadata and rendered README
        run: uvx --from twine==7.0.0 twine check dist/advect-*

      - name: Upload release distributions
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: advect-release-distributions-${{ needs.validate.outputs.version }}
          path: |
            dist/advect-*.whl
            dist/advect-*.tar.gz
          if-no-files-found: error

      - name: Upload release provenance
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: advect-release-provenance-${{ needs.validate.outputs.version }}
          path: provenance
          if-no-files-found: error

  testpypi:
    name: Publish candidate to TestPyPI
    if: github.event_name == 'workflow_dispatch'
    needs: [validate, assemble]
    runs-on: ubuntu-22.04
    environment:
      name: testpypi
      url: https://test.pypi.org/p/advect
    permissions:
      id-token: write # TestPyPI trusted publishing exchanges the OIDC identity
    steps:
      - name: Download release distributions
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: advect-release-distributions-${{ needs.validate.outputs.version }}
          path: dist

      - name: Publish distributions with trusted publishing
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # release/v1
        with:
          packages-dir: dist
          repository-url: https://test.pypi.org/legacy/

  verify-testpypi:
    name: Verify TestPyPI installation
    needs: [validate, testpypi]
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          enable-cache: false

      - name: Install and smoke-test the candidate
        env:
          RELEASE_VERSION: ${{ needs.validate.outputs.version }}
        run: |
          uv venv .testpypi-smoke --python 3.14
          uv pip install \
            --python .testpypi-smoke/bin/python \
            --no-cache \
            'numpy>=2.0,<2.6' \
            'array-api-compat>=1.11.2,<2' \
            'scipy>=1.18,<2' \
            'xarray>=2024.6'
          uv pip install \
            --python .testpypi-smoke/bin/python \
            --no-cache \
            --no-deps \
            --index-url https://test.pypi.org/simple/ \
            "advect==$RELEASE_VERSION"
          uv pip check --python .testpypi-smoke/bin/python
          .testpypi-smoke/bin/python scripts/smoke_wheel_install.py scientific

  docs:
    name: Publish versioned documentation
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [validate, assemble]
    uses: ./.github/workflows/docs.yml
    with:
      version: ${{ needs.validate.outputs.version }}
    permissions:
      contents: write # the reusable docs workflow pushes gh-pages

  github-release:
    name: Publish GitHub release
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [validate, assemble, docs]
    runs-on: ubuntu-22.04
    permissions:
      contents: write # this job creates the GitHub release
    steps:
      - name: Download release distributions
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: advect-release-distributions-${{ needs.validate.outputs.version }}
          path: release

      - name: Download release provenance
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: advect-release-provenance-${{ needs.validate.outputs.version }}
          path: release

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ github.ref_name }}
          RELEASE_VERSION: ${{ needs.validate.outputs.version }}
        run: >-
          gh release create "$RELEASE_TAG"
          release/*
          --verify-tag
          --generate-notes
          --title "Advect $RELEASE_VERSION"

  pypi:
    name: Publish to PyPI
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [validate, assemble, github-release]
    runs-on: ubuntu-22.04
    environment:
      name: pypi
      url: https://pypi.org/p/advect
    permissions:
      id-token: write # PyPI trusted publishing exchanges the OIDC identity
    steps:
      - name: Download release distributions
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: advect-release-distributions-${{ needs.validate.outputs.version }}
          path: dist

      - name: Publish distributions with trusted publishing
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # release/v1
        with:
          packages-dir: dist

  verify-pypi:
    name: Verify public installation
    needs: [validate, pypi]
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          enable-cache: false

      - name: Install the published package
        env:
          RELEASE_VERSION: ${{ needs.validate.outputs.version }}
        run: |
          uv venv .pypi-smoke --python 3.14
          uv pip install \
            --python .pypi-smoke/bin/python \
            --no-cache \
            "advect==$RELEASE_VERSION"
          .pypi-smoke/bin/python scripts/smoke_wheel_install.py base
