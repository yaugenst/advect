"""NumPy ``__array_function__`` binding, emission, and handler ownership.

This package owns executable protocol dispatch.  Public support declarations
and their independent evidence remain outside this package and must not be
derived from its handler registry.
"""
